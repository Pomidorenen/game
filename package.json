{
	"homepage": "http://pomidorenen.github.io/game",
	"name": "mygame",
	"type": "module",
	"scripts": {
		"build": "vite build",
		"deploy": "gh-pages -d dist",
		"dev": "vite --host",
		"preview": "vite preview",
		"zip": "npm run build && mkdir -p dist && zip -r dist/game.zip www -x \"**/.DS_Store\""
	},
	"dependencies": {
		"kaplay": "^3001.0.0-beta.5"
	},
	"devDependencies": {
		"gh-pages": "^6.2.0",
		"vite": "^5.4.9"
	}
}
